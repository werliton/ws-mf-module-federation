{
  "name": "ws-mf-module-federation",
  "private": true,
  
  "scripts": {
    "start": "lerna run --parallel dev",
    "build": "lerna run build"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.2.2"
  }
}
