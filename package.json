{
  "name": "ws-mf-module-federation",
  "private": true,
  
  "scripts": {
    "dev": "lerna run --parallel dev",
    "build": "lerna run build"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.2.2"
  }
}
